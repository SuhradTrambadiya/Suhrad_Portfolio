import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  env: {
    EMAIL_USER: "suhrad.patel.1111@gmail.com",
    EMAIL_PASS: "muse paof wzog wxbb",
  },
};

export default nextConfig;
